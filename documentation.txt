
LV2 - 2d vector class
variables: x, y
functions:
	toString
	copy
	setAs
	setValues
	add/iadd
	sub/isub
	scale/iscale
	div/idiv
	dot
	dist
	mag
	round
	floor

UI:
	LBox
	variables: elem (the jq element), subs (sub components)
	functions: hide, show, add (adds subcomponent)
	
	LGrid
	variables: elem (the jq element), subs, width and height

	LLabel
	variables: elem

	LButton
	variables: elem
	
